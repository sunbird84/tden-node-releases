name: Publish TDEN node candidate

on:
  workflow_dispatch:
    inputs:
      release_channel:
        description: Preview for testing; stable only after complete deployment acceptance
        required: true
        type: choice
        options:
          - preview
          - stable
        default: preview
      candidate_run_id:
        description: Successful Build TDEN node candidate workflow run ID
        required: true
        type: string
      expected_sha256:
        description: Exact candidate archive SHA-256; stable requires completed acceptance
        required: true
        type: string

permissions:
  actions: read
  attestations: read
  contents: write

concurrency:
  group: node-release-${{ inputs.release_channel }}
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment: ${{ inputs.release_channel == 'stable' && 'stable-node-release' || 'preview-node-release' }}
    env:
      GH_TOKEN: ${{ github.token }}
      CANDIDATE_RUN_ID: ${{ inputs.candidate_run_id }}
      EXPECTED_SHA256: ${{ inputs.expected_sha256 }}
      RELEASE_CHANNEL: ${{ inputs.release_channel }}
    steps:
      - name: Check out release repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Verify candidate workflow origin
        id: origin
        shell: bash
        run: |
          set -euo pipefail
          [[ "$CANDIDATE_RUN_ID" =~ ^[1-9][0-9]*$ ]]
          [[ "$EXPECTED_SHA256" =~ ^[0-9a-f]{64}$ ]]
          [[ "$RELEASE_CHANNEL" == preview || "$RELEASE_CHANNEL" == stable ]]
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$CANDIDATE_RUN_ID" > run.json
          workflow_id="$(python3 -c 'import json; print(json.load(open("run.json"))["workflow_id"])')"
          [[ "$workflow_id" =~ ^[1-9][0-9]*$ ]]
          gh api "repos/$GITHUB_REPOSITORY/actions/workflows/$workflow_id" > workflow.json
          python3 - <<'PY'
          import json, os, re
          run = json.load(open('run.json', encoding='utf-8'))
          workflow = json.load(open('workflow.json', encoding='utf-8'))
          expected = {
              'id': int(os.environ['CANDIDATE_RUN_ID']),
              'event': 'workflow_dispatch',
              'status': 'completed',
              'conclusion': 'success',
          }
          for key, value in expected.items():
              if run.get(key) != value:
                  raise SystemExit(f'candidate workflow {key} mismatch')
          if run.get('head_repository', {}).get('full_name') != os.environ['GITHUB_REPOSITORY']:
              raise SystemExit('candidate workflow repository mismatch')
          if workflow.get('id') != run.get('workflow_id') or workflow.get('path') != '.github/workflows/build-node-candidate.yml':
              raise SystemExit('candidate workflow definition mismatch')
          if not re.fullmatch(r'[0-9a-f]{40}', run.get('head_sha', '')) or type(run.get('run_attempt')) is not int or run['run_attempt'] < 1:
              raise SystemExit('candidate workflow commit/attempt is invalid')
          PY
          attempt="$(python3 -c 'import json; print(json.load(open("run.json"))["run_attempt"])')"
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/runs/$CANDIDATE_RUN_ID/attempts/$attempt/jobs?per_page=100" > candidate-jobs.json
          python3 - <<'PY'
          import os
          from scripts.candidate_metadata import needs_runtime, read_json
          required = needs_runtime(read_json('candidate-jobs.json'))
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'runtime_bundle={str(required).lower()}\n')
          PY

      - name: Download exact candidate artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir candidate
          gh run download "$CANDIDATE_RUN_ID" \
            --repo "$GITHUB_REPOSITORY" \
            --name "tden-node-candidate-$CANDIDATE_RUN_ID" \
            --dir candidate
          find candidate -mindepth 1 -maxdepth 1 -type f -printf '%f\n' | LC_ALL=C sort > candidate-files.txt
          diff -u <(printf '%s\n' candidate-build.json tden-node-linux-amd64.tar.gz tden-node-linux-amd64.tar.gz.sha256 | LC_ALL=C sort) candidate-files.txt

      - name: Verify GitHub-hosted build provenance
        shell: bash
        run: |
          set -euo pipefail
          source_sha="$(python3 -c 'import json; print(json.load(open("run.json"))["head_sha"])')"
          gh attestation verify candidate/tden-node-linux-amd64.tar.gz \
            --repo "$GITHUB_REPOSITORY" \
            --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/build-node-candidate.yml" \
            --signer-digest "$source_sha" --source-digest "$source_sha" \
            --deny-self-hosted-runners --format json > archive-verification.json

      - name: Verify attested runtime build description
        if: steps.origin.outputs.runtime_bundle == 'true'
        shell: bash
        run: |
          set -euo pipefail
          source_sha="$(python3 -c 'import json; print(json.load(open("run.json"))["head_sha"])')"
          gh attestation verify candidate/candidate-build.json \
            --repo "$GITHUB_REPOSITORY" \
            --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/build-node-candidate.yml" \
            --signer-digest "$source_sha" --source-digest "$source_sha" \
            --deny-self-hosted-runners --format json > description-verification.json

      - name: Verify accepted digest and build description
        id: describe
        shell: bash
        run: |
          set -euo pipefail
          (cd candidate && sha256sum --strict --check tden-node-linux-amd64.tar.gz.sha256)
          actual_sha256="$(sha256sum candidate/tden-node-linux-amd64.tar.gz | cut -d' ' -f1)"
          archive_bytes="$(stat -c %s candidate/tden-node-linux-amd64.tar.gz)"
          [[ "$actual_sha256" == "$EXPECTED_SHA256" ]]
          python3 scripts/candidate_metadata.py verify
          version="$(python3 -c 'import json; print(json.load(open("candidate/candidate-build.json"))["version"])')"
          version_slug="$(printf '%s' "$version" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g; s/^-+|-+$//g; s/-+/-/g')"
          release_tag="node-${version_slug}-run-${CANDIDATE_RUN_ID}-${actual_sha256}"
          printf 'archive_sha256=%s\n' "$actual_sha256" >> "$GITHUB_OUTPUT"
          printf 'archive_bytes=%s\n' "$archive_bytes" >> "$GITHUB_OUTPUT"
          printf 'release_tag=%s\n' "$release_tag" >> "$GITHUB_OUTPUT"

      - name: Retain provenance verification records
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: node-provenance-verification-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            run.json
            workflow.json
            candidate-jobs.json
            archive-verification.json
            description-verification.json
          if-no-files-found: warn
          retention-days: 30

      - name: Publish the exact archive without rebuilding
        shell: bash
        env:
          ARCHIVE_SHA256: ${{ steps.describe.outputs.archive_sha256 }}
          ARCHIVE_BYTES: ${{ steps.describe.outputs.archive_bytes }}
          RELEASE_TAG: ${{ steps.describe.outputs.release_tag }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, os, pathlib
          build = json.load(open('candidate/candidate-build.json', encoding='utf-8'))
          body = (
              f"Release channel: {os.environ['RELEASE_CHANNEL']}\n"
              f"Archive SHA-256: {os.environ['ARCHIVE_SHA256']}\n"
              f"Archive bytes: {os.environ['ARCHIVE_BYTES']}\n"
              f"Candidate workflow run: {build['workflow_run_id']}\n"
              f"Chain commit: {build['chain_commit']}\n"
              f"Gateway commit: {build['gateway_commit']}\n"
              f"Deploy commit: {build['deploy_commit']}\n"
              f"Bootstrap trust template SHA-256: {build['bootstrap_trust_template_sha256']}\n"
              f"TUF root SHA-256: {build['tuf_root_sha256']}\n"
              "\nPublication verified the official build origin and the exact archive digest. "
              "The archive was not rebuilt. Deployment acceptance results are recorded separately.\n"
          )
          if os.environ['RELEASE_CHANNEL'] == 'preview':
              body += (
                  "\nFor controlled installation testing only; full deployment acceptance is incomplete. "
                  "This is not a stable release, TUF promotion or DAO upgrade approval. "
                  "Enable test releases in Node Console. Existing nodes and accounts are unchanged.\n"
                  "\n仅供受控安装测试，完整部署验收尚未完成。不是稳定版、TUF 晋升或 DAO 升级批准。"
                  "请在控制台勾选包含测试版；本次发布不改变已有节点和账户。\n"
              )
          if 'runtime_bundle' in build:
              runtime = build['runtime_bundle']
              body += (
                  f"\nPackage manifest SHA-256: {runtime['package_manifest_sha256']}\n"
                  f"Package metadata SHA-256: {runtime['package_metadata_sha256']}\n"
                  f"Configuration profile SHA-256: {runtime['configuration_profile_sha256']}\n"
                  f"Runtime helper SHA-256: {runtime['helper']['sha256']}\n"
                  f"Build description has a separate GitHub attestation from run {build['workflow_run_id']} "
                  f"attempt {build['workflow_run_attempt']}, workflow commit {build['workflow_commit']}.\n"
                  "This binds the original archive and source repository revisions; it is not an offline DAO release signature or whole-node acceptance.\n"
              )
          pathlib.Path('release-notes.txt').write_text(body, encoding='utf-8')
          PY
          # Published previews keep their original assets; only verified metadata is promoted.
          # 已发布预览版保留原附件；验签与摘要复核通过后仅晋升发布元数据。
          release_action=create
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" \
              --json isDraft,isPrerelease > existing-release.json 2>/dev/null; then
            release_action="$(python3 - <<'PY'
          import os
          from scripts.candidate_metadata import existing_release_action, read_json
          print(existing_release_action(os.environ['RELEASE_CHANNEL'], read_json('existing-release.json')))
          PY
          )"
          else
            flags=(--draft --latest=false)
            if [[ "$RELEASE_CHANNEL" == preview ]]; then flags+=(--prerelease); fi
            gh release create "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" \
              --title "$RELEASE_TAG" --notes-file release-notes.txt "${flags[@]}"
          fi
          if [[ "$release_action" != promote_preview ]]; then
            gh release upload "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --clobber \
              candidate/tden-node-linux-amd64.tar.gz \
              candidate/tden-node-linux-amd64.tar.gz.sha256 \
              candidate/candidate-build.json
          fi
          gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --json assets > uploaded-assets.json
          python3 - <<'PY'
          from scripts.candidate_metadata import read_json, verify_release_assets
          verify_release_assets(read_json('uploaded-assets.json')['assets'], 'candidate')
          PY
          if [[ "$RELEASE_CHANNEL" == preview ]]; then
            gh release edit "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" \
              --draft=false --prerelease --latest=false --notes-file release-notes.txt
          else
            gh release edit "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" \
              --draft=false --prerelease=false --latest --notes-file release-notes.txt
          fi
